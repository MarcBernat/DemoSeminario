# DemoSeminario
Demostración para el dia 12 de mayo de 2107

# Comandos 
Para inicar Docker Compose:
```
$ sudo docker-compose up -d
```
Para eliminar todos los contenedores:
```
$ sudo docker rm -f $(sudo docker ps -a -q)
```
